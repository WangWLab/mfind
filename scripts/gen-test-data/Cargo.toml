[package]
name = "gen-test-data"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full", "rt-multi-thread", "macros", "fs", "sync"] }
futures = "0.3"
rand = "0.8"
anyhow = "1.0"
clap = { version = "4", features = ["derive"] }
